---
title: "Типы материалов"
# The folder holds the pages that live at /library/types/<type>; it is not a
# section of its own and has nothing to show at /types.
# Each sibling's `title` is the plural form and must equal
# data/creative_types.yaml's `label_plural` for that type — the page lists
# many materials of one type, so its heading reads as a group, not one item.
build:
  render: never
  list: never
---
